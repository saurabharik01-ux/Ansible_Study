---
- name: set password
  hosts: all
  become: true
  tasks:
    - name: set password
      user:
        name: abc
        update_password: always
        password: "{{ 'abcd123' | password_hash('sha512') }}"
